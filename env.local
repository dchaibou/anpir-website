# .env.local

# Configuration SMTP (Exemple avec Gmail ou un service SMTP dédié)
SMTP_EMAIL=contact@anpir-niger.org # L'adresse d'envoi
SMTP_PASSWORD=******************** # Le mot de passe ou la clé d'application
SMTP_HOST=smtp.gmail.com           # ou smtp-relay.sendinblue.com, smtp.sendgrid.net, etc.
SMTP_PORT=587 # Port TLS standard

# Destination de l'e-mail (où les demandes de contact arrivent)
CONTACT_EMAIL_DESTINATION=bureau.anpir@anpir-niger.org